# PDF text extraction
pymupdf>=1.24

# OCR — Google Cloud Vision (DOCUMENT_TEXT_DETECTION). Switched from
# self-hosted EasyOCR after real testing showed ~90s/page on CPU;
# Vision API is faster, more accurate, and ~$1.50 per 1,000 pages
# (first 1,000/month free) — see ingestion/ocr.py docstring.
google-cloud-vision>=3.7

# Embeddings (BGE-M3: dense + sparse + ColBERT multi-vector)
FlagEmbedding>=1.3

# Vector store
qdrant-client>=1.12

# LLM cleanup/structuring
groq>=0.11

# Env loading (reads the repo's root .env.local)
python-dotenv>=1.0
