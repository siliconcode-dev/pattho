# PDF text extraction
pymupdf>=1.24

# OCR — Google Cloud Vision (DOCUMENT_TEXT_DETECTION). Switched from
# self-hosted EasyOCR after real testing showed ~90s/page on CPU;
# Vision API is faster, more accurate, and ~$1.50 per 1,000 pages
# (first 1,000/month free) — see ingestion/ocr.py docstring.
google-cloud-vision>=3.7

# Embeddings (BGE-M3: dense + ColBERT multi-vector — see embed.py docstring
# for why sparse is intentionally left off)
FlagEmbedding>=1.3

# Vector store — Weaviate Cloud (free tier: 10GB, no card, native
# hybrid search + ColBERT-style multivector). Third choice 2026-09-08
# after Qdrant Cloud's 4GB ran out and LanceDB hit real storage-backend
# dead ends (R2 needs a card, B2 lacks conditional-PUT support Lance's
# commit protocol requires, GCS risks real billing) — see
# weaviate_store.py docstring.
weaviate-client>=4.16

# LLM cleanup/structuring
groq>=0.11

# Env loading (reads the repo's root .env.local)
python-dotenv>=1.0
